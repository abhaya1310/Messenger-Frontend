# Backend API URL (required - frontend calls backend directly)
# Set this to your backend URL
# 
# Development (local):
#   NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
#
# Production (Vercel / cloud backend):
#   NEXT_PUBLIC_BACKEND_URL=https://csat-cloud.vercel.app
#   Or your custom backend domain: https://api.yourdomain.com
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# Default organization ID for multitenancy (optional)
# NEXT_PUBLIC_DEFAULT_ORG_ID=demo-org

# ----------------------------------------------------------------------------
# Server-side environment variables (NOT exposed to the browser)
# Used by Next.js API routes under app/api as a proxy to the backend.
# ----------------------------------------------------------------------------
# BACKEND_URL: Base URL for the backend used by server-side API routes
#   Development:
#     BACKEND_URL=http://localhost:3000
#   Production (example):
#     BACKEND_URL=https://csat-cloud.vercel.app
BACKEND_URL=http://localhost:3000

ADMIN_PORTAL_USERNAME=
ADMIN_PORTAL_PASSWORD=
ADMIN_SESSION_SECRET=
ADMIN_SESSION_TTL_SECONDS=3600

# ============================================================================
# CLOUD DEPLOYMENT NOTES (Vercel, AWS, etc.)
# ============================================================================
#
# When deploying the frontend:
#
# 1. Set NEXT_PUBLIC_BACKEND_URL to your public backend URL
#    Example (Vercel backend): https://csat-cloud.vercel.app
#
# 2. Set BACKEND_URL and ADMIN_TOKEN as server-side env vars
#    in your deployment platform (Vercel Project Settings -> Environment).
#
# 3. Update backend CORS configuration:
#    - Add your frontend URL(s) to backend's ALLOWED_ORIGINS env var
#    - Example: ALLOWED_ORIGINS=http://localhost:3001,https://your-frontend.vercel.app
#    - This must be done in the backend repository
#
# 4. Verify CORS is working:
#    - Check browser console for CORS errors
#    - Test API endpoints from the deployed frontend
#
# ============================================================================
