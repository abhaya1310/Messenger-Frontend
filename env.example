# Backend API URL (required - frontend calls backend directly)
# Set this to your backend URL (e.g., http://localhost:3000 for dev, https://api.yourdomain.com for production)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# Admin token for protected endpoints (optional for development)
# This should match the ADMIN_TOKEN in your backend .env file
# Leave empty for dev mode if backend allows unauthenticated requests
NEXT_PUBLIC_ADMIN_TOKEN=

# Default organization ID for multitenancy (optional)
# NEXT_PUBLIC_DEFAULT_ORG_ID=demo-org
